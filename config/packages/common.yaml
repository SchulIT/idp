parameters:
  project_url: https://github.com/schulit/idp

common:
  app:
    url: "%env(APP_URL)%"
    name: "%env(APP_NAME)%"
    version: "%version%"
    project_url: "%project_url%"
    logo: "%env(APP_LOGO)%"
  locales: "%locales%"
  menu: mainMenu
  templates:
    mail: '_mail/index.html.twig'
    logs: '_logs/index.html.twig'
    clear_logs: '_logs/clear.html.twig'