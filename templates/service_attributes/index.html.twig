{% extends "main.html.twig" %}

{% block title %}{{ 'service_attributes.label'|trans }}{% endblock %}

{% block content %}
    <div class="contentbox">
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <colgroup>
                    <col class="column-lg" />
                    <col class="column-lg" />
                    <col />
                    <col class="column-lg" />
                </colgroup>

                <thead>
                <tr>
                    <th>{{ 'label.name'|trans }}</th>
                    <th>{{ 'label.saml_attribute_name'|trans }}</th>
                    <th>{{ 'label.services'|trans }}</th>
                    <th>{{ 'actions.label'|trans }}</th>
                </tr>
                </thead>

                <tbody>
                {% for attribute in attributes %}
                    <tr>
                        <td>
                            {{ attribute.name }}

                            <p><small>{{ attribute.description }}</small></p>
                        </td>
                        <td>{{ attribute.samlAttributeName }}</td>
                        <td>
                            <ul class="list-unstyled">
                                {% for service in attribute.services %}
                                    <li title="{{ service.description }}">{{ service.name }}</li>
                                {% endfor %}
                            </ul>
                        </td>
                        <td>
                            <a href="{{ path('edit_attribute', { id: attribute.id }) }}" class="btn btn-sm btn-primary">
                                {{ 'actions.edit'|trans }}
                            </a>

                            <a href="{{ path('remove_attribute', { id: attribute.id }) }}" class="btn btn-sm btn-danger">
                                {{ 'actions.remove'|trans }}
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <div class="buttons">
        <a href="{{ path('add_attribute') }}" class="btn btn-sm btn-primary">
            <i class="fa fa-plus"></i> {{ 'service_attributes.add.label'|trans }}
        </a>
    </div>
{% endblock%}