{% extends "@Common/simple/main.html.twig" %}

{% block title %}{{ 'error.label'|trans }}{% endblock %}

{% block content %}
    <div class="text-center">
        <i class="fas fa-ban fa-3x d-block text-secondary"></i>
        <h4>{{ 'sso.error.title'|trans }}</h4>
        <p>{{ 'sso.error.message'|trans }}</p>
        <pre>{{ message }}</pre>
    </div>

    <hr >

    <p>{{ 'sso.denied.help'|trans }}</p>

    <hr >

    <a href="{{ path('dashboard') }}" class="btn btn-primary w-100">{{ 'dashboard.link'|trans }}</a>
{% endblock %}