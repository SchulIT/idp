{{ 'greeting'|trans({}, 'mail') }}

{{ 'registration.link'|trans({'%link%': url('confirm_registration_code', { token: token })}, 'mail') }}
{{ 'registration.expiry'|trans({'%date%': expiry_date|format_date()}, 'mail') }}

{{ 'farewell'|trans({}, 'mail') }}

-----
{{ 'notice'|trans({}, 'mail') }}