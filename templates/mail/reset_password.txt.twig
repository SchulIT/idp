{{ 'greeting'|trans({}, 'mail') }}

{{ 'reset_password.message'|trans({'%username%': username }, 'mail') }}

{{ 'reset_password.link'|trans({'%link%': url('change_password', { token: token.token })}, 'mail') }}

{{ 'reset_password.expiry'|trans({'%date%': token.expiresAt|format_datetime}, 'mail') }}

{{ 'farewell'|trans({}, 'mail') }}

-----
{{ 'notice'|trans({}, 'mail') }}