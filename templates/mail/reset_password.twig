{{ 'greeting'|trans({'%firstname%': user.firstname, '%lastname%': user.lastname }, 'mail') }}

{{ 'reset_password.message'|trans({'%link%': url('change_password', { token: token.token })}, 'mail') }}
{{ 'reset_password.expiry'|trans({'%date%': token.expiresAt|format_date()}, 'mail') }}

{{ 'farewell'|trans({}, 'mail') }}

-----
{{ 'notice'|trans({}, 'mail') }}