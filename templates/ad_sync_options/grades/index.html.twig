{% extends "main.html.twig" %}

{% block title %}{{ 'ad_sync_options.grades.label'|trans }}{% endblock %}

{% block content %}
    <div class="contentbox">
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <colgroup>
                    <col />
                    <col class="column-lg" />
                    <col class="column-lg" />
                </colgroup>

                <thead>
                <tr>
                    <th>{{ 'label.grade'|trans }}</th>
                    <th>{{ 'label.source'|trans }}</th>
                    <th>{{ 'actions.label'|trans }}</th>
                </tr>
                </thead>

                <tbody>
                {% for option in sync_options %}
                    <tr>
                        <td>
                            {{ option.grade }}
                        </td>
                        <td>
                            <span class="badge badge-primary">{% if option.sourceType == 'ou' %}{{ 'label.ou'|trans }}{% else %}{{ 'label.ad_group'|trans }}{% endif %}</span>
                            {{ option.source }}
                        </td>
                        <td>
                            <a href="{{ path('edit_ad_grades_sync_options', { id: option.id }) }}" class="btn btn-sm btn-primary">
                                {{ 'actions.edit'|trans }}
                            </a>

                            <a href="{{ path('remove_ad_grades_sync_options', { id: option.id }) }}" class="btn btn-sm btn-danger">
                                {{ 'actions.remove'|trans }}
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <div class="buttons">
        <a href="{{ path('add_ad_grades_sync_options') }}" class="btn btn-sm btn-primary">
            <i class="fa fa-plus"></i> {{ 'ad_sync_options.grades.add.label'|trans }}
        </a>

        <a href="{{ path('ad_sync_options') }}" class="btn btn-sm btn-secondary">
            <i class="fa fa-arrow-left"></i> {{ 'actions.back'|trans }}
        </a>
    </div>
{% endblock%}